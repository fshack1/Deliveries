<%= form_with(model: delivery, data: { controller: "form" }) do |form| %>
  <% if delivery.errors.any? %>
    <div style="color: var(--color-status-error)">
      <h2><%= pluralize(delivery.errors.count, "error") %> prohibited this delivery from being saved:</h2>
      <ul>
        <% delivery.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mdc-text-field mdc-text-field--filled">
    <span class="mdc-text-field__ripple"></span>
    <%= form.text_field(:pickup_address, class: "mdc-text-field__input", required: true) %>
    <span class="mdc-floating-label">Pickup address</span>
    <span class="mdc-line-ripple"></span>
  </div>

  <div class="mdc-text-field mdc-text-field--filled">
    <span class="mdc-text-field__ripple"></span>
    <%= form.text_field(:delivery_address, class: "mdc-text-field__input", required: true) %>
    <span class="mdc-floating-label">Delivery address</span>
    <span class="mdc-line-ripple"></span>
  </div>

  <div class="mdc-text-field mdc-text-field--filled">
    <span class="mdc-text-field__ripple"></span>
    <%= form.number_field(:weight, class: "mdc-text-field__input", min: 0.001, max: 50000, step: "any", required: true) %>
    <span class="mdc-floating-label">Weight (kg) </span>
    <span class="mdc-line-ripple"></span>
  </div>

  <div class="mdc-text-field mdc-text-field--filled">
    <span class="mdc-text-field__ripple"></span>
    <%= form.number_field(:distance, class: "mdc-text-field__input", min: 0.001, max: 100000000, step: "any", required: true) %>
    <span class="mdc-floating-label">Distance (km) </span>
    <span class="mdc-line-ripple"></span>
  </div>

  <div class="mdc-text-field mdc-text-field--filled">
    <span class="mdc-text-field__ripple"></span>
    <%= form.datetime_field(:scheduled_time, class: "mdc-text-field__input", min: Time.current.strftime("%Y-%m-%dT%H:%M"), max: (Time.current + 3.years).strftime("%Y-%m-%dT%H:%M"), required: true) %>
    <span class="mdc-floating-label">Scheduled time</span>
    <span class="mdc-line-ripple"></span>
  </div>

  <div class="mdc-text-field mdc-text-field--filled">
    <span class="mdc-text-field__ripple"></span>
    <%= form.text_field(:driver_name, class: "mdc-text-field__input") %>
    <span class="mdc-floating-label">Driver name</span>
    <span class="mdc-line-ripple"></span>
  </div>

  <div class="form-button">
    <%= form.submit("Submit", class:"mdc-button mdc-button--raised")%>
  </div>
<% end %>
